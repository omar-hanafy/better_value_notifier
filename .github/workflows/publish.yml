name: Publish to pub.dev

on:
  push:
    tags:
      - 'better_value_notifier-v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
